<div class="border-b-2 flex items-center justify-between">
  <%= render partial: "writings/header/date", locals: {writing:} %>

  <% if on_page?("artists", "show") || on_page?("writings", "search") %>
    <div id="tag_list" class="flex mb-1">
      <%= render partial: 'tags/tag_list', locals: { writing:} %>
    </div>
  <% end %>

  <%= render partial: "writings/header/actions", locals: {writing:} %>
</div>
