<div>
  <%= turbo_frame_tag("writing_#{writing.id}_alert") %>
  <button data-modal-target="#modal_<%= writing.id %>" class="modal-open">
    <i class="fa-solid fa-edit text-indigo-900"></i>
  </button>
  <%= render partial: "shared/modal_form", locals: {
             modal: "modal_#{writing.id}",
             title: writing.date,
             form_partial: "writings/form",
             form_locals: { artist:, writing:}  } %>
</div>
