<% if on_page?("writings", "index") || on_page?("writings", "search") || on_page?("writings", "update") %>
  <%= writing.content %>
  <%= render "tags/add_tag", url: artist_writing_tags_path(@artist, writing, taggable_type: "writing") %>
<% else %>
  <%= truncate(writing.content.to_plain_text, length: 200) %>
<% end %>
