<% if controller_name == "writings" %>
  <%= writing.content %>
  <%= render "tags/add", url: artist_writing_tags_path(@artist, writing, taggable_type: "writing") if user_signed_in? %>
<% else %>
  <%= truncate(writing.content.to_plain_text, length: 200) %>
<% end %>
