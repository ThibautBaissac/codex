<main id="main" data-sidebar-target="main" class="flex-1 mb-4 pt-3 overflow-y-auto md:ml-72 relative" role="main">
  <header class="fixed top-0 bg-sky-950 shadow-md z-10 w-full flex items-center px-3 py-2 border-t-4 border-orange-700">
    <button class="focus:outline-none md:hidden z-50 mr-2" id="menuButton" aria-label="Open menu" data-action="click->sidebar#toggleSidebar">
      <svg id="hamburgerIcon" data-sidebar-target="hamburgerIcon" class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24"
        xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
      </svg>
    </button>
    <i class="fas fa-hippo mr-2 text-white"></i><span class="text-white text-lg font-semibold hidden md:block"></i>Codex</span>
  </header>
  <div class="bg-sky-950">
    <div class="bg-white md:rounded-tl-2xl">
      <div class="mt-8 p-2 <%= width %>">
        <%= turbo_frame_tag "alerts" do %>
          <%= render partial: "shared/alerts/alerts" %>
        <% end %>
        <%= yield %>
      </div>
    </div>
  </div>
</main>
