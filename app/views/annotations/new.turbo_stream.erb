<%= turbo_stream.replace "remote_modal" do %>
  <%= render ModalComponent.new(title: t("annotations.add_annotation")) do %>
    <%= render partial: 'annotations/form', locals: { annotation: @annotation, annotatable_type: "writing", annotatable_id: @writing.id, url: artist_writing_annotations_path(@writing.artist, @writing) } %>
  <% end %>
<% end %>
