<%= form_with(model: Annotation.new, url: , method: :post) do |form| %>
  <%= form.rich_text_area :content, placeholder: t("annotations.add_annotation") %>
  <%= hidden_field_tag "annotation[annotatable_type]", annotatable_type %>
  <%= hidden_field_tag "annotation[annotatable_id]", annotatable_id %>
  <%= form.submit t("default.save"), class: "mt-2 bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded-xl focus:outline-none focus:shadow-outline" %>
<% end %>
