<div class="col-lg-8 mt-3 mx-auto">
  <div class="d-flex justify-content-between align-items-center">
    <h1>Œuvres</h1>
    <% if current_user %>
      <%= link_to "#form_work_modal",
        data: { bs_toggle: "modal", bs_target: "#form_work_modal"}, class: "small d-flex align-items-center text-decoration-none" do %>
        <%= icon('fa-solid', 'plus-circle', class: "me-1") %> Créer une œuvre
      <% end %>
    <% end %>
  </div>

  <% @works.each do |work| %>
    <%= render partial: "work_card", locals: { work: work } %>
  <% end %>
</div>

<%= render partial: "form_work_modal" %>
