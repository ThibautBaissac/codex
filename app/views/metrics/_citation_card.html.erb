<% elements.each do |element| %>
  <%= link_to(element_path(element), class: "text-dark text-decoration-none") do %>
    <div class="card">
      <%= simple_format(element.title) %>
      <% if element.mouvements.any?  %>
        <ul>
          <% element.mouvements.each do |mouvement| %>
            <li><%= simple_format(mouvement.work.title) %> - <%= simple_format(mouvement.title) %></li>
          <% end %>
        </ul>
      <% end %>
    </div>
  <% end %>
<% end %>
